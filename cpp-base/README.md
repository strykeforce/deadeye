# cpp-base

Makes the `j3ff/deadeye-cpp-base` Docker image that Deadeye daemon can be built with. The `aarch64` version is for building a Jetson image and uses the Nvidia runtime and the provided OpenCV and GStreamer. The `x86_64` compiles in a version of OpenCV and GStreamer.
